// use is creating and switching to a new db
// db.nameof new collection.insert( object );
// show collection displays the models
// db.name.find() shows the contents, findOne() shows the first one
// db.remove(select criteria is id#)
// db.name.update(id, object)